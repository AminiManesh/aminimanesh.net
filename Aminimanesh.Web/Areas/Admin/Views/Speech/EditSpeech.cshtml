@using Aminimanesh.DataLayer.Entities.Owner
@model Speech
@{
    ViewData["Title"] = "Edit Speech";
}

<!-- Page Heading -->
<h1 class="h3 mb-2 text-gray-800">@ViewData["Title"]</h1>
<p class="mb-4">
    Edit this speech for your resume.
</p>

<div class="card shadow mb-4">
    <div class="card-header py-3 bg-info">
        <h6 class="m-0 font-weight-bold text-white">Speech Information</h6>
    </div>
    <div class="card-body">
        <form class="user" asp-area="Admin" asp-controller="Speech" asp-action="EditSpeech" method="post">

            @if (!ViewData.ModelState.IsValid)
            {
                <div class="alert alert-danger" data-bs-theme="dark" asp-validation-summary="All"></div>
            }

            <input type="hidden" asp-for="@Model.SpeechId" value="@Model.SpeechId" />

            <div class="form-group row">
                <input type="text" class="form-control" asp-for="@Model.Text" value="@Model.Text"
                       placeholder="Speech Content">
            </div>

            <div class="form-group row">
                <div class="col-3">
                    <input type="number" class="form-control" asp-for="@Model.Priority" min="1" value="@Model.Priority"
                           placeholder="Priority">
                </div>
            </div>

            <button type="submit" class="btn btn-success btn-user btn-block">
                Edit Speech
            </button>
        </form>
    </div>
</div>

